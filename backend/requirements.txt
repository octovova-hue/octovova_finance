# ============================================================
# Octovova Finance Planning Engine — Backend Dependencies
# ============================================================

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0           # Postgres async driver
aiosqlite>=0.20.0         # SQLite async driver (local dev)
alembic>=1.13.0

# Pydantic & Settings
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.1.0    # EmailStr support

# Auth
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# HTTP client (Monte Carlo + LLM Hub calls)
httpx>=0.27.0

# Env
python-dotenv>=1.0.0

# Mako (Alembic template engine)
mako>=1.3.0
